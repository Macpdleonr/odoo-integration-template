<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="res_config_settings_view_form" model="ir.ui.view">
            <field name="name">res.config.settings.view.form.inherit</field>
            <field name="model">res.config.settings</field>
            <field name="inherit_id" ref="base.res_config_settings_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//form" position="inside">
                    <app
                        name="settings"
                        string="settings"
                        logo="/{{cookiecutter.module_name}}/static/description/logo.png">
                        <block
                            title="Setting Credentials"
                            name="credentials">
                            <setting
                                id="username"
                                string="Username"
                                help="Username">
                                <field name="username"/>
                            </setting>
                            <setting
                                id="password"
                                string="Password"
                                help="Password">
                                <field name="password" password="true" class="oe_inline"/>
                            </setting>
                        </block>
                    </app>
                </xpath>
            </field>
        </record>

        <record id="config_settings" model="ir.actions.act_window">
            <field name="name">Settings</field>
            <field name="res_model">res.config.settings</field>
            <field name="view_mode">form</field>
            <field name="target">inline</field>
            <field name="context">{'module' : 'settings', 'bin_size': False}</field>
        </record>
    </data>
</odoo>